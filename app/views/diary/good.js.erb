// このファイルは DiaryCard コンポーネント (frontend/components/DiaryCard/DiaryCard.tsx) に移行済み。
// Ajax DOM 更新は React の useState (goodCount / myGood) で代替されている。
// diary_controller#good は respond_to で JSON: head :ok を返す。
